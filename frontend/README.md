# Frontend — Skone IT Ticketing

## Setup

1. Install dependencies:
   ```bash
   cd frontend
   npm install
   ```
2. Start the React app:
   ```bash
   npm start
   ```

The frontend proxies API calls to `http://localhost:4000` by default.
